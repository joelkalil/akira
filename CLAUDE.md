# Akira Development Notes

Akira follows the project direction documented in `docs/AKIRA_PLAN.md`.

For now, keep implementation work scoped to the active issue. The v1.0 target is
a Python CLI distributed through PyPI using a `src` package layout.

